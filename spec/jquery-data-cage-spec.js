// vim settings
// :set ts=2
// :set sw=2
// :set expandtab

describe("DataCage", function() {

  describe ("initial configuration", function() {
    it ("should have the possibility to define a commit, rollback and cancel actions on initialization", function() {
       expect(false).toBe(true);
    });
  });

  describe ("define a data cage in a form element",function() {

    it ("should define a data cage in a form element", function() {
        expect(false).toBe(true);
    });

    it ("should call form submit as default action in a commit for a form element",function() {
        expect(false).toBe(true);
    } );

  });


  it ("should define a data cage in a non-form element", function() {
      expect(false).toBe(true);
  });

  it ("should show confirmation when clicking out of a changed area", function() {
      expect(false).toBe(true);
  });
 



});
